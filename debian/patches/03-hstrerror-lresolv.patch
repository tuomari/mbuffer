Description: Do not look for hstrerror(3) in libresolv.
 The libraries needed for hstrerror(3) have already been determined above.
Forwarded: yes
Author: Peter Pentchev <roam@ringlet.net>
Last-Update: 2009-12-10

--- a/configure
+++ b/configure
@@ -4445,6 +4445,7 @@
   as_fn_error "could not find the library containing name convertion functions!" "$LINENO" 5
 fi
 
+if false; then
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking for hstrerror in -lresolv" >&5
 $as_echo_n "checking for hstrerror in -lresolv... " >&6; }
 if test "${ac_cv_lib_resolv_hstrerror+set}" = set; then :
@@ -4489,6 +4490,7 @@
   LIBS="-lresolv $LIBS"
 
 fi
+fi
 
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking for library containing sendfile" >&5
 $as_echo_n "checking for library containing sendfile... " >&6; }
